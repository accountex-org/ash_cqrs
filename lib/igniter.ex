# TODO: Create installation process via Igniter.

if Code.ensure_loaded?(Igniter) do
  defmodule AshCqrs.Igniter do
    @moduledoc "Codemods and utilities for working with AshCqrs & Igniter"

    @doc false
  end
end
